# GitHub Personal Access Token with `read:packages` scope.
# Used by .npmrc to install @happilee-app packages from GitHub Packages.
# Copy to .env and set your token locally — never commit real tokens.
NODE_AUTH_TOKEN=
